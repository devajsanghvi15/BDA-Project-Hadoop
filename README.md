Upload repository_data.csv to HDFS (directory name: input_dir)

query1: Top 1000 repositories sorted by number of stars

query2: Number of repositories by primary language used

query3: Total number of stars per year

query4: Total number of forks per year

query5: Total number of pull requests per year

query6: Total number of watchers per language

query7: Top 1000 repositories sorted by number of pull requests

query8: Average number of stars for repositories, grouped by their primary programming language.

clean-all-jar-class.bat: Deletes all .jar and .class files

delete-hadoop-folders.bat: Deletes all hadoop output folders (query1 to query8)